# Copy this file to .env and fill in at least one LLM provider key.
# The active provider is set via "llm_provider" in config.json (default: minimax).

# LLM providers — set the key for whichever provider(s) you use
MINIMAX_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
KIMI_API_KEY=
