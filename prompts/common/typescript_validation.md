BEFORE COMMITTING — run validation:
- Build: `npm run build`
- Lint: `npm run lint`
- Tests: if the project has a test script or test files, run `npm test -- --runInBand` or the project-specific test command
Only commit and push after required commands pass.
