BEFORE COMMITTING — run validation:
- Tests: python -m pytest (or python -m py_compile <file.py> if no tests exist)
- Linting: python -m ruff check <file.py> (install with: python -m pip install ruff)
Only commit and push after both pass.
